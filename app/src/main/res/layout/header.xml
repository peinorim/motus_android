<?xml version="1.0" encoding="utf-8"?>
<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="fill_parent"
    android:theme="@style/DrawerToolbar"
    android:layout_height="wrap_content"
    android:background="@color/teal"
    android:orientation="vertical"
    android:weightSum="1">

    <LinearLayout
        android:id="@+id/header_top"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="@dimen/activity_vertical_margin"
        android:orientation="vertical">

        <TextView
            android:id="@+id/appinfo"
            android:layout_width="fill_parent"
            android:layout_height="70sp"
            android:layout_marginLeft="@dimen/min_text_size"
            android:gravity="center_vertical|left"
            android:layout_weight="2"
            android:text="@string/blank"
            android:textColor="#000000"
            android:drawableLeft="@mipmap/ic_launcher"
            android:scaleGravity="fill_vertical"
            android:drawablePadding="10sp"
            android:textSize="@dimen/normal_text_size"
            android:textStyle="bold"/>
    </LinearLayout>

    <LinearLayout
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_below="@+id/header_top"
        android:orientation="vertical">

        <TextView
            android:id="@+id/credits"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_marginLeft="@dimen/min_text_size"
            android:layout_marginBottom="@dimen/min_text_size"
            android:layout_marginTop="@dimen/min_text_size"
            android:text="@string/blank"
            android:textColor="#000000"
            android:textSize="@dimen/min_text_size"
            android:textStyle="normal"/>
    </LinearLayout>
</RelativeLayout>